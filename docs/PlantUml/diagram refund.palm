@startuml
start

:Запрос возврата;

:Поиск исходного чека;

if (Чек найден?) then (Нет)
  :Отказ в возврате;
  :Конец возврата;
  stop
else (Да)
  :Формирование чека возврата;
  :Фискализация возврата;
  :Возврат денег;
  :Конец возврата;
  stop
endif

@enduml